// Previsão do tempo Juquitiba
async function carregarClima() {
  const climaDiv = document.getElementById('clima');
  try {
    const response = await fetch('https://api.open-meteo.com/v1/forecast?latitude=-23.932&longitude=-47.065&current_weather=true');
    const data = await response.json();
    const clima = data.current_weather;
    climaDiv.innerHTML = `🌤️ ${clima.temperature}°C - ${clima.weathercode === 0 ? "Céu limpo" : "Parcialmente nublado"}`;
  } catch (error) {
    climaDiv.textContent = "☁️ Clima indisponível";
  }
}
carregarClima();

// Interação com o Juka
const juka = document.getElementById('juka-chat');
juka.addEventListener('click', () => {
  alert("Oi, eu sou o Juka! 🦫 Explore Juquitiba comigo!");
});
